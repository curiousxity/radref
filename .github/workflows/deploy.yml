name: Deploy

# Push to main builds and publishes to Cloudflare. Mirrors the local commands
# exactly, so a green local `npm run build` is a green deploy.
on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  # A newer push supersedes an in-flight deploy rather than racing it.
  group: deploy
  cancel-in-progress: true

jobs:
  deploy:
    # Pinned so an image change is a reviewed commit, not a surprise on some later push.
    runs-on: ubuntu-26.04
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: npm
      - run: npm ci
      - run: npm run lint
      # The study and calculator rules are pinned by tests; a changed threshold fails here.
      - run: npm test
      # `npm run build` is `tsc -b && vite build`, so a type error fails here
      # rather than shipping.
      - run: npm run build
      - uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          wranglerVersion: '4'
